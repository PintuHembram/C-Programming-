	#include <stdio.h>
	#includ <conio.h>
	void main() 
	{
	    int a[5], i;
	
	    clrscr();  //This Faction Only Used To Turbu++
	    printf("Enter 5 elements to the array \n");
	    
	    for (i = 0; i < 5; i++) 
	    {
	        scanf("%d", &a[i]);
	    }
	    
	    printf("\n The elements of the array are \n");
	    
	    for (i = 0; i < 5; i++) 
	    {
	    printf("\n a[%d] = %d", i, a[i]);
	    }
	}
